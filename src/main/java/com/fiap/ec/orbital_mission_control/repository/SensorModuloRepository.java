package com.fiap.ec.orbital_mission_control.repository;

import com.fiap.ec.orbital_mission_control.model.SensorModulo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SensorModuloRepository extends JpaRepository<SensorModulo, Long> {
}
